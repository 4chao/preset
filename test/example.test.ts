describe('example', () => {
  // it('1 + 1', () => {
  //   assert(1 + 1 === ('2' as any))
  // })
  // it('Complex calculation', () => {
  //   assert(
  //     ((() => ({
  //       abc: '123bbb',
  //     }))().abc.split('bbb')[0] as any) === parseInt('123'),
  //   )
  // })

  it('Correct 1 + 1 ', () => {
    assert(1 + 1 === 2)
  })
})
